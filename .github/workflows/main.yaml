name: Docker Image CI

on:
  workflow_dispatch:
    
permissions:
  contents: read
  pull-requests: write

jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: List files
      run: ls -R

